# Server and DB runtime config (Key=Value)
# CLI flags override env vars, env vars override this file.

PORT=8080
PUBLIC_DIR=./server/public

DB_PATH=./data/app.db
DB_SCHEMA=./db/schema.sql
DB_SEED_FILE=./db/seed.sql
DB_ENABLE_SEED=1

# SQLite options
DB_JOURNAL_MODE=WAL
DB_FOREIGN_KEYS=1
DB_BUSY_TIMEOUT_MS=5000
